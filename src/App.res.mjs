// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Core__Array from "@rescript/core/src/Core__Array.res.mjs";
import * as JsxRuntime from "react/jsx-runtime";

function getCharWidth(c) {
  switch (c) {
    case " " :
    case "i" :
    case "l" :
        return 1;
    case "f" :
    case "r" :
    case "t" :
        return 2;
    case "m" :
    case "w" :
        return 4;
    default:
      return 3;
  }
}

function App$WordIcon(props) {
  var size = props.size;
  var text = props.text;
  var scaleText = function (text) {
    var measure = Core__Array.reduce(text.split(""), 0, (function (acc, c) {
            return acc + getCharWidth(c) | 0;
          }));
    var applied = measure <= 12 ? measure + (12 - measure) * 0.5 : measure;
    return 0.13 * Math.pow(applied, -0.5);
  };
  return JsxRuntime.jsx("div", {
              children: JsxRuntime.jsx("div", {
                    children: text,
                    style: {
                      fontSize: (scaleText(text) * size).toString() + "em",
                      fontWeight: "900",
                      letterSpacing: "-0.05em",
                      lineHeight: "0.65",
                      textAlign: "center",
                      width: size.toString() + "px",
                      overflowWrap: "break-word"
                    }
                  }),
              style: {
                display: "flex",
                height: size.toString() + "px",
                width: size.toString() + "px",
                alignItems: "center",
                flexDirection: "row",
                justifyItems: "center"
              }
            });
}

function App(props) {
  return JsxRuntime.jsx("div", {
              children: JsxRuntime.jsx("div", {
                    children: [
                        "Menu",
                        "Close",
                        "Settings",
                        "Delete",
                        "Hang Up",
                        "Search",
                        "Home",
                        "Zoom",
                        "Edit",
                        "back"
                      ].map(function (text) {
                          return JsxRuntime.jsxs("div", {
                                      children: [
                                        JsxRuntime.jsx(App$WordIcon, {
                                              text: text,
                                              size: 20
                                            }),
                                        JsxRuntime.jsx("div", {
                                              children: "Search",
                                              className: "h-5 text-lg"
                                            })
                                      ],
                                      className: " p-2 border rounded m-2 border-gray-300 shadow flex flex-row items-center gap-2"
                                    });
                        }),
                    className: "flex flex-row max-w-lg flex-wrap"
                  }),
              className: "p-6 "
            });
}

var make = App;

export {
  make ,
}
/* react/jsx-runtime Not a pure module */
